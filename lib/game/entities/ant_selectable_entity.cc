#include "ant_selectable_entity.h"

void AntSelectableEntity::setupRing()
{
/*


  def setupRing
    @ring=getRing
    return if @ring.nil?
    if @selected
      #f6c108
      @ring.setRingColor(AGVector4.new(1,0.7,0.1,0.8))
    else
      @ring.setRingColor(AGVector4.new(0.7,0.7,1,0.8))
    end
    addMesh(@ring,AGVector3.new(0,0,0))
    #@ring.setVisible(false)
    updateRingColor
  end
  */
}
